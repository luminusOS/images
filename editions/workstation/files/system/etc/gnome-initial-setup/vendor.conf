[pages]
# Sirius already selects the installed system language. Keep the keyboard page
# in GNOME Initial Setup so the user can confirm or change the XKB layout.
skip=software
existing_user_only=language
